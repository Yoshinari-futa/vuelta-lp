# VUELTA LP — 本番公開手順（正）

このファイルが **LP デプロイの単一ソース**です。内容が変わったらここだけ更新する。

## 前提

| 項目 | 内容 |
|------|------|
| **このモノレポでのパス** | `VUELTA_ROOT/Webアプリ/lp`（例: `~/Desktop/VUELTA/Webアプリ/lp`） |
| **GitHub** | `Yoshinari-futa/vuelta-lp`（LP 単体リポジトリ。作業は clone 先でも同じ手順） |
| **本番URL（例）** | `https://vuelta-lp.vercel.app`（Vercel の Production に表示される URL が正） |
| **ホスティング** | Vercel（Next.js 前提） |

※ 過去のドキュメントにあった `~/lp` など別パスは、**clone 先が違うだけ**で手順は同じ。

---

## いつも使う流れ（GitHub 連携済みのとき・推奨）

1. LP リポジトリで変更をコミットする。
2. `main`（または Vercel が追っているブランチ）へ **push**。
3. Vercel の **Deployments** で新しいビルドが走るのを確認する（数分）。
4. 下記「公開後チェック」を踏む。

**プッシュできないとき**は GitHub の認証（PAT / GitHub Desktop / `gh auth`）を確認する。

---

## 初回・別環境：Vercel にまだ繋いでいないとき

### A. ダッシュボードから Import（いちばんわかりやすい）

1. [Vercel](https://vercel.com) にログイン（GitHub 連携推奨）。
2. **Add New → Project** → GitHub の `vuelta-lp` を Import。
3. Framework: **Next.js**、Root Directory: **`./`**、Build: `npm run build`（自動検出でよい）。
4. **Deploy**。完了後に `.vercel.app` の URL をメモする。

### B. CLI（フォールバック）

```bash
cd "$VUELTA_ROOT/Webアプリ/lp"   # または vuelta-lp の clone 先
npx vercel login                  # 初回のみ
npx vercel --prod
```

---

## 手動で再デプロイだけしたいとき

- Vercel ダッシュボード → 該当プロジェクト → **Deployments** → 最新デプロイの **⋯** から **Redeploy**。

---

## 公開後チェック

- [ ] `/`・`/ja`・`/recruit` など主要ページが表示される
- [ ] モバイル / デスクトップで崩れがない
- [ ] Instagram 等の外部リンクが動く
- [ ] 地図・電話・営業時間が意図どおり
- [ ] 必要なら [リッチリザルトテスト](https://search.google.com/test/rich-results) で構造化データを確認

---

## トラブルシューティング

| 症状 | まず見る |
|------|----------|
| ビルド失敗 | Vercel の **Build Logs**、ローカルで `npm run build` |
| 表示が古い | デプロイが最新コミットか確認、ブラウザキャッシュ／シークレットで再確認 |
| フォント・画像 | `npm run build` が通るか、`public/` 配下のパス |

---

## 環境変数

- **値はリポジトリに書かない。** 必要なキーは Vercel の **Settings → Environment Variables** のみ。
- 過去にドキュメントへ貼られていたキーは **無効化・ローテーション**を推奨（漏洩扱い）。

---

## カスタムドメイン

Vercel → **Settings → Domains** で追加し、表示される DNS 指示に従う（`vuelta.jp` 等は別途レジストラ側設定）。

---

*最終更新: 2026-04-10*
